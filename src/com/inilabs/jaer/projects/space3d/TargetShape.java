package com.inilabs.jaer.projects.space3d;

/** Shapes supported by FlyingBlobGenerator when injecting synthetic events. */
public enum TargetShape {
    CIRCLE,
    SQUARE,
    TRIANGLE,   // equilateral, upright
    CROSS       // plus sign with bar width ~= radius/4
}
